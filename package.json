{
  "license": "MIT",
  "name": "ticketchain",
  "private": true,
  "repository": "git@github.com:javierlinked/ticketchain.git",
  "author": "@javierlinked",
  "scripts": {
    "build": "cd packages/hardhat && yarn build && cd ../app && yarn build",
    "dev": "yarn workspace app dev",
    "dev:all": "yarn workspaces foreach -Apt run dev",
    "test": "yarn workspaces foreach -Apt run test",
    "vercel-build": "cd packages/app && yarn build"
  },
  "version": "0.2.0",
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@4.10.3"
}
